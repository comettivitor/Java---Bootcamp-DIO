public class Atividades {
    public static void main(String[] args) {
        // NomeIdade nomeIdade = new NomeIdade();
        // nomeIdade.verificaNomeIdade();

        // Nota nota = new Nota();
        // nota.calculaNotas();

        // MaiorMedia maiorMedia = new MaiorMedia();
        // maiorMedia.calculaValores();

        // ParImpar parImpar = new ParImpar();
        // parImpar.verificaParImpar();

        // Tabuada tabuada = new Tabuada();
        // tabuada.calculaTabuada();

        // Fatorial fatorial = new Fatorial();
        // fatorial.calculaFatorial();

        // Inverso inverso = new Inverso();
        // inverso.inverteArray();

        // Consoantes consoantes = new Consoantes();
        // consoantes.leConsoantes();

        // Numeros numeros = new Numeros();
        // numeros.numerosAleatorios();

        // Matriz matriz = new Matriz();
        // matriz.geraMatriz();

        // Multiplos multiplos = new Multiplos();
        // multiplos.somaMultiplos();

        // Recursao recursão = new Recursao();
        // System.out.println(recursão.somatorio(15));

        // FizzBuzz fb = new FizzBuzz();
        // fb.fizzBuzz(15);

        BuscaSequencial buscaSequencial = new BuscaSequencial();
        System.out.println(buscaSequencial.confereVetor(-9));
    }
}
